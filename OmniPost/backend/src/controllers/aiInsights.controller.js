// Controller for the planned AI-driven post-performance analysis feature.
export async function listInsights(req, res) {
  // Real implementation: AIInsight.listForUser(req.user.id)
  // (the insights themselves would be generated by services/aiAnalysis.service.js, not here)
  res.status(501).json({ error: 'Not implemented — prototype stage', endpoint: 'GET /api/insights' });
}
